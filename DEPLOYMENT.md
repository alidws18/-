# 🚀 دليل النشر الشامل - نظام التقييم المركزي

هذا الدليل يشرح خطوة بخطوة كيفية نشر النظام على Netlify مع Supabase.

---

## 📋 ما ستحتاجه

- ✅ حساب GitHub (مجاني)
- ✅ حساب Supabase (مجاني)
- ✅ حساب Netlify (مجاني)
- ✅ Node.js مثبت على جهازك

---

## المرحلة 1️⃣: إعداد Supabase

### الخطوة 1: إنشاء مشروع Supabase

1. **انتقل إلى** [Supabase](https://supabase.com)
2. **انقر** على "Start your project"
3. **سجل دخول** بحساب GitHub الخاص بك
4. **انقر** "New Project"
5. **أدخل البيانات:**
   ```
   Project Name: gov-evaluation-system
   Database Password: [اختر كلمة مرور قوية واحفظها]
   Region: West Asia (Bahrain) - الأقرب للسعودية
   Pricing Plan: Free
   ```
6. **انقر** "Create new project"
7. **انتظر** 2-3 دقائق حتى يكتمل الإعداد

### الخطوة 2: احصل على مفاتيح الـ API

1. من Dashboard، انقر على **Settings** (أيقونة الترس)
2. انقر على **API**
3. ستجد:
   ```
   Project URL: https://xxxxx.supabase.co
   anon public key: eyJhbG...
   service_role key: eyJhbG... (لا تستخدم هذا)
   ```
4. **انسخ** وحفظ:
   - Project URL
   - anon public key

### الخطوة 3: إنشاء قاعدة البيانات

1. من القائمة الجانبية، انقر **SQL Editor**
2. انقر **New Query**
3. **انسخ والصق** محتوى ملف `database-schema.sql` بالكامل
4. انقر **Run** (أو اضغط Ctrl+Enter)
5. ستظهر رسالة "Success" - هذا يعني تم إنشاء جميع الجداول

### الخطوة 4: تطبيق سياسات الأمان

1. في SQL Editor، افتح **New Query** جديد
2. **انسخ والصق** محتوى ملف `rls-policies.sql` بالكامل
3. انقر **Run**
4. ستظهر رسالة "Success"

### الخطوة 5: إنشاء أول مستخدم مدير

1. من القائمة الجانبية، انقر **Authentication**
2. انقر **Users**
3. انقر **Add user** → **Create new user**
4. أدخل:
   ```
   Email: admin@gov.sa (أو أي بريد تريده)
   Password: [كلمة مرور قوية]
   Auto Confirm User: ✅ (مهم!)
   ```
5. انقر **Create user**
6. **انسخ** الـ UUID الخاص بالمستخدم (ستجده في العمود الأول)

7. ارجع إلى **SQL Editor** وقم بتشغيل:
   ```sql
   INSERT INTO users (id, role_id, full_name, email, is_active)
   SELECT 
     'الصق-UUID-هنا',  -- استبدل بالـ UUID الذي نسخته
     (SELECT id FROM roles WHERE name = 'admin'),
     'مدير النظام',
     'admin@gov.sa',
     true;
   ```

8. الآن لديك مستخدم مدير جاهز!

---

## المرحلة 2️⃣: إعداد المشروع محلياً

### الخطوة 1: تحميل الملفات

1. **افتح** Terminal/Command Prompt
2. **انتقل** إلى مجلد المشروع:
   ```bash
   cd path/to/gov-evaluation-system
   ```

### الخطوة 2: تثبيت الحزم

```bash
npm install
```

سيستغرق هذا 2-3 دقائق لتحميل جميع المكتبات.

### الخطوة 3: إعداد متغيرات البيئة

1. **انسخ** ملف البيئة:
   ```bash
   cp .env.example .env
   ```

2. **افتح** ملف `.env` في محرر نصوص
3. **عدّل** القيم:
   ```env
   VITE_SUPABASE_URL=https://xxxxx.supabase.co
   VITE_SUPABASE_ANON_KEY=eyJhbG...
   ```
   (استخدم القيم التي نسختها من Supabase)

### الخطوة 4: تجربة المشروع محلياً

```bash
npm run dev
```

1. افتح المتصفح على `http://localhost:3000`
2. جرّب تسجيل الدخول بالبريد وكلمة المرور التي أنشأتها
3. يجب أن تظهر لوحة تحكم المدير

🎉 **إذا نجح هذا، أنت جاهز للنشر!**

---

## المرحلة 3️⃣: رفع الكود إلى GitHub

### الخطوة 1: إنشاء Repository

1. اذهب إلى [GitHub](https://github.com)
2. انقر **New repository**
3. أدخل:
   ```
   Repository name: gov-evaluation-system
   Description: نظام التقييم المركزي لإدارة خدمة العملاء
   Private: ✅ (لأنه مشروع حكومي)
   ```
4. انقر **Create repository**

### الخطوة 2: رفع الملفات

في Terminal:

```bash
# تهيئة Git
git init

# إضافة جميع الملفات
git add .

# أول Commit
git commit -m "Initial commit: Government Evaluation System"

# ربط مع GitHub (استبدل USERNAME بحسابك)
git remote add origin https://github.com/USERNAME/gov-evaluation-system.git

# رفع الملفات
git push -u origin main
```

**⚠️ مهم:** تأكد من عدم رفع ملف `.env` (موجود في `.gitignore`)

---

## المرحلة 4️⃣: النشر على Netlify

### الطريقة الأسهل: ربط مباشر مع GitHub

#### الخطوة 1: إنشاء حساب Netlify

1. اذهب إلى [Netlify](https://www.netlify.com)
2. انقر **Sign up**
3. اختر **GitHub** لتسجيل الدخول
4. وافق على الصلاحيات

#### الخطوة 2: إنشاء موقع جديد

1. من Dashboard، انقر **Add new site** → **Import an existing project**
2. اختر **GitHub**
3. **ابحث** عن repository: `gov-evaluation-system`
4. **انقر** على اسم الـ repository

#### الخطوة 3: تكوين البناء

في صفحة "Site settings":

```
Branch to deploy: main
Build command: npm run build
Publish directory: dist
```

**لا تنقر Deploy بعد!**

#### الخطوة 4: إضافة متغيرات البيئة

1. **انقر** على "Advanced build settings"
2. **انقر** "New variable"
3. **أضف:**
   ```
   Key: VITE_SUPABASE_URL
   Value: https://xxxxx.supabase.co
   
   Key: VITE_SUPABASE_ANON_KEY
   Value: eyJhbG...
   ```

#### الخطوة 5: النشر!

1. **انقر** "Deploy site"
2. **انتظر** 2-3 دقائق
3. سترى "Site is live" عندما ينتهي

#### الخطوة 6: الحصول على الرابط

سيعطيك Netlify رابط مثل:
```
https://random-name-123.netlify.app
```

---

## المرحلة 5️⃣: تخصيص الرابط (اختياري)

### تغيير الاسم

1. من Dashboard، انقر **Site settings**
2. انقر **Change site name**
3. أدخل اسم مخصص:
   ```
   gov-evaluation-system
   ```
4. الآن الرابط: `https://gov-evaluation-system.netlify.app`

### إضافة نطاق مخصص (للإنتاج)

إذا كان لديك نطاق حكومي مثل `evaluation.gov.sa`:

1. من Site settings، انقر **Domain management**
2. انقر **Add custom domain**
3. أدخل: `evaluation.gov.sa`
4. اتبع التعليمات لتحديث DNS

---

## 🎯 التحقق النهائي

### قائمة التحقق ✅

- [ ] قاعدة البيانات تعمل على Supabase
- [ ] جميع الجداول موجودة (11 جدول)
- [ ] RLS Policies مفعّلة
- [ ] مستخدم مدير موجود
- [ ] المشروع يعمل محلياً
- [ ] الكود على GitHub
- [ ] الموقع منشور على Netlify
- [ ] متغيرات البيئة مضافة
- [ ] يمكن تسجيل الدخول

### تجربة النظام

1. **افتح** رابط Netlify
2. **سجل دخول** بحساب المدير:
   ```
   Email: admin@gov.sa
   Password: [كلمة المرور التي أنشأتها]
   ```
3. **تحقق من:**
   - [ ] لوحة التحكم تظهر
   - [ ] يمكن الوصول لصفحة إدارة النماذج
   - [ ] يمكن إضافة مركز جديد
   - [ ] يمكن إضافة مستخدم جديد

---

## 🔄 التحديثات المستقبلية

عندما تريد تحديث الموقع:

```bash
# عدّل الملفات التي تريدها

# احفظ التعديلات
git add .
git commit -m "وصف التعديل"
git push

# Netlify سيقوم بالنشر تلقائياً!
```

---

## 🚨 استكشاف الأخطاء

### المشكلة: "Build failed"

**الحل:**
1. تحقق من logs في Netlify
2. تأكد من تثبيت جميع الحزم
3. جرّب `npm run build` محلياً

### المشكلة: "Cannot read environment variables"

**الحل:**
1. تأكد من إضافة متغيرات البيئة في Netlify
2. تحقق من عدم وجود مسافات زائدة
3. أعد النشر

### المشكلة: صفحة بيضاء بعد النشر

**الحل:**
1. افتح Developer Console (F12)
2. ابحث عن الأخطاء
3. تحقق من صحة رابط Supabase
4. تحقق من RLS policies

### المشكلة: لا يمكن تسجيل الدخول

**الحل:**
1. تحقق من إنشاء المستخدم في Supabase
2. تأكد من تأكيد البريد (Auto Confirm User)
3. تحقق من إضافة المستخدم لجدول `users`
4. تحقق من RLS policies

---

## 📱 الاستخدام على الموبايل

النظام responsive ويعمل على:
- ✅ Desktop
- ✅ Tablet
- ✅ Mobile

لكن يُنصح باستخدام Desktop للإدارة.

---

## 🔐 الأمان

### نصائح مهمة:

1. **لا تشارك** مفاتيح Supabase
2. **غيّر** كلمة مرور المدير بعد أول استخدام
3. **فعّل** 2FA على GitHub وNetlify
4. **راجع** سجلات التدقيق بانتظام
5. **حدّث** المكتبات دورياً:
   ```bash
   npm update
   ```

---

## 📞 الدعم

إذا واجهت مشاكل:

1. **راجع** هذا الدليل أولاً
2. **تحقق من** logs في:
   - Netlify: Deploy logs
   - Supabase: Database logs
   - Browser: Developer Console
3. **اتصل بـ** الدعم الفني

---

## 🎓 موارد إضافية

- [Supabase Docs](https://supabase.com/docs)
- [Netlify Docs](https://docs.netlify.com)
- [React Docs](https://react.dev)
- [Tailwind CSS](https://tailwindcss.com/docs)

---

**🎉 تهانينا! نظامك الآن منشور ويعمل!**

التاريخ: 2026
نسخة الدليل: 1.0
